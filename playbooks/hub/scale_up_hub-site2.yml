- name: Bring up Hub Site 2.
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Scale Hub CR replicas according to role. 
      redhat.openshift.k8s:
        state: present
        api_version: automationhub.ansible.com/v1beta1
        kind: AutomationHub
        name: hub-aws-01
        namespace: aap-24-dr
        merge_type: merge 
        definition:
          spec:
            web:
              replicas: 1
            content:
              replicas: 2
            api: 
              replicas: 1
            redis: 
              replicas: 1
            worker: 
              replicas: 2
