apiVersion: v1
kind: Service
metadata:
  name: aap-site-metrics
  namespace: "{{ site_namespace }}"
  labels:
    app: aap-site-metrics
spec:
  selector:
    app: aap-site-metrics
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      protocol: TCP
